{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "deploy": "eb init --profile default && eb deploy"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "mongoose": "^6.1.7",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "engines": {
    "node": "16.13.1"
  }
}
